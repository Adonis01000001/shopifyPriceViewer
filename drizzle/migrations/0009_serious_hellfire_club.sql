-- The Price Radar schema is established by 0008_price_radar.sql.
-- This journal entry was generated from the same schema and intentionally
-- remains a no-op so existing migration ordering is preserved without
-- attempting to recreate tables and indexes.
SELECT 1;
